import 'package:flutter/material.dart';

class MuhasabaScreen extends StatelessWidget {
  const MuhasabaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('محاسبة النفس')),
      body: const Center(child: Text('شاشة محاسبة النفس - قيد التطوير')),
    );
  }
}
